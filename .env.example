APP_ENV=development
APP_NAME=MyGuest API v2
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/myguestv2
CORS_ORIGINS=["http://localhost:3000","http://localhost:8081"]
CORS_ALLOW_CREDENTIALS=false
TRUSTED_HOSTS=["localhost","127.0.0.1","testserver"]
# In production, set TRUSTED_HOSTS to the real API hostname(s), for example:
# TRUSTED_HOSTS=["api.myguestapp.com"]
# API_DOCS_ENABLED=false
FIREBASE_CREDENTIALS_PATH=
FIREBASE_STORAGE_BUCKET=client-keeper-a2e91.appspot.com
